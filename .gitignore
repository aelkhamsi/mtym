node_modules
dist
.turbo
.DS_Store
Makefile
# Vim swap files (can contain secrets)
*.swp
*.swo
*~
apps/front/organizer-photos/
apps/front/partner-logos/

# local security PoC / notes — not for the repo
security/

# local docker compose overrides
docker-compose.override.yaml

# local build/db backups made by deploy & rollback scripts — never commit
*.bak
*.bak.*
.next.bak.*
dist.bak.*
*.dump
